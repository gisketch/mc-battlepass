# Spawning And Worlds

## Worlds

- `ckdm:cozy_world`: cozy player world. `/setworldspawn` is allowed here.
- `ckdm:sky_lands`: Sky Lands. Uses Sky Archipelago `sky_archipelago:sky_island` terrain based on the built-in `from_the_lowest_low_to_the_highest_high` survival preset.
- `minecraft:overworld`: normal base overworld. Vanilla-compatible terrain, mobs, and difficulty behavior stay unchanged.

## Sky Lands Rules

- Login does not force-teleport players to CKDM worlds. Players stay where Minecraft loads them.
- Bedless death respawn uses the dimension last selected by `/setworldspawn` in `minecraft:overworld`, `ckdm:cozy_world`, or `ckdm:sky_lands`.
- Natural mob spawns are blocked in Sky Lands for cozy hub use.
- Sky Lands has no ocean layer.
- Sky Lands uses the survival preset as a base, with middle-ground island size bands for living space: small `40-52`, medium `55-78`, large `90-135`, with dynamic `150-250` cluster spacing.
- Sky Islands are generated above Y `128`; the fall-through trigger stays down at Y `48` so normal mining below an island has a clear air gap before transfer.
- Falling below Y `48` in Sky Lands transfers the player to `minecraft:overworld` at the same X/Z and Y `320`.
- Fall-through transfer preserves falling velocity so a paraglider or other fall-control tool matters.
- Rising to Y `360` or above in `minecraft:overworld` transfers the player back to Sky Lands at the same X/Z and Y `64`.
- Command/spawn-egg/admin-created entities are not blocked by the natural spawn rule.
- No hub pad or forced spawn platform is generated by the mod.

## Commands

```mcfunction
/ck worlds status [player]
/ck worlds cozy_world [player]
/ck worlds cozy [player]
/ck worlds sky_lands [player]
/ck worlds hub [player]
/ck worlds overworld [player]
/ck worlds base [player]
```

`cozy_world` and `cozy` teleport to the Cozy World shared spawn. `sky_lands` and `hub` teleport to the Sky Lands shared spawn. `overworld` and `base` teleport to the normal overworld spawn. These travel commands do not set personal respawn points.

Vanilla `/setworldspawn` is overridden to allow `minecraft:overworld`, `ckdm:cozy_world`, and `ckdm:sky_lands`. The selected dimension is saved in `config/gisketchs_chowkingdom_mod/worlds/spawn.toml`.

## Dependencies

Sky Lands needs the Sky Archipelago mod present on server startup. The dimension JSON uses generator type `sky_archipelago:sky_island`.

The base terrain values were copied from Sky Archipelago's built-in preset at `data/sky_archipelago/presets/from_the_lowest_low_to_the_highest_high.json`. CKDM overrides it by disabling the ocean layer, raising minimum island Y to keep the fall-through band clear, and gently increasing island size bands for better settlement space.

## Biome Mods

Sky Lands uses this biome source:

```json
{
	"type": "minecraft:multi_noise",
	"preset": "minecraft:overworld"
}
```

This is the best compatibility path for biome mods like Regions Unexplored or Oh The Biomes We've Gone when they extend the normal overworld biome preset. If a biome mod only injects into the literal `minecraft:overworld` dimension key, that mod may not affect `ckdm:sky_lands`; that behavior is controlled by the biome mod, not CKDM.

Rule: biome mods that modify the overworld biome preset should appear in Sky Lands; biome mods that target only the vanilla overworld dimension may need an extra compatibility datapack or mod-specific config.
