# Spawning And Worlds

## Worlds

- `ckdm:cozy_world`: cozy player world. `/setworldspawn` is allowed here.
- `ckdm:sky_lands`: Sky Lands. Uses Sky Archipelago `sky_archipelago:sky_island` terrain based on the built-in `from_the_lowest_low_to_the_highest_high` survival preset.
- `minecraft:overworld`: normal base overworld. Vanilla-compatible terrain, mobs, and difficulty behavior stay unchanged.

## Sky Lands Rules

- Login does not force-teleport players to CKDM worlds. Players stay where Minecraft loads them.
- Bedless death respawn uses the dimension last selected by `/setworldspawn` in `minecraft:overworld`, `ckdm:cozy_world`, or `ckdm:sky_lands`.
- World travel and respawn use the nearest safe spawn surface around the dimension shared spawn instead of blindly placing the player at the raw spawn coordinate.
- Natural mob spawns are blocked in Sky Lands for cozy hub use.
- Sky Lands has no ocean layer.
- Sky Lands uses the survival preset as a base, tuned for flatter visible floating settlement islands: small radius `52-67`, medium `71-101`, large `116-175`, with dynamic `190-320` cluster spacing and island centers from Y `96-230`.
- Sky Lands is the cozy home layer. Villages can remain rare peaceful discoveries, but hostile, boss, dungeon, underground-only, ocean-only, and oversized adventure structures are denied by the Sky Archipelago structure policy. The denylist includes vanilla danger structures plus scraped modpack structures from Cataclysm, MineCells, YUNG's structure mods, Block Factory's Bosses, FDBosses, Witcher RPG hideouts/graves, Better Archeology catacombs/underwater ruins, Supplementaries galleons, and the largest/underground Structory entries.
- Sky island centers are generated from Y `96` to Y `230`; the fall-through trigger stays down at Y `48` so normal mining below an island has a clear air gap before transfer.
- Falling below Y `48` in Sky Lands transfers the player to `minecraft:overworld` at the same X/Z and Y `520`.
- Fall-through transfer preserves falling velocity so a paraglider or other fall-control tool matters.
- Rising to Y `600` or above in `minecraft:overworld` transfers the player back to Sky Lands at the same X/Z and Y `64` only when that Sky Lands column has a safe surface; otherwise it uses the Sky Lands shared spawn. The gap between Overworld Y `520` and Y `600` prevents a fall-through arrival from immediately bouncing back to Sky Lands.
- Command/spawn-egg/admin-created entities are not blocked by the natural spawn rule.
- No hub pad or forced spawn platform is generated by the mod.

## Commands

```mcfunction
/ck worlds status [player]
/ck worlds cozy_world [player]
/ck worlds cozy [player]
/ck worlds sky_lands [player]
/ck worlds hub [player]
/ck worlds overworld [player]
/ck worlds base [player]
```

`cozy_world` and `cozy` teleport to the Cozy World shared spawn. `sky_lands` and `hub` teleport to the Sky Lands shared spawn. `overworld` and `base` teleport to the normal overworld spawn. These travel commands do not set personal respawn points.

Vanilla `/setworldspawn` is overridden to allow `minecraft:overworld`, `ckdm:cozy_world`, and `ckdm:sky_lands`. The selected dimension is saved in `config/gisketchs_chowkingdom_mod/worlds/spawn.toml`.

## Dependencies

Sky Lands needs the Sky Archipelago mod present on server startup. The dimension JSON uses generator type `sky_archipelago:sky_island`.

The base terrain values were copied from Sky Archipelago's built-in preset at `data/sky_archipelago/presets/from_the_lowest_low_to_the_highest_high.json`. CKDM overrides it by disabling the ocean layer, flattening terrain relief, using vertical island variety, and increasing island radii for settlement space.

For normal deployment, the mod-bundled `data/ckdm/dimension/sky_lands.json` defines `ckdm:sky_lands`; do not copy the standalone world datapack unless server testing proves a world-level override is needed. With Sky Archipelago `1.3.0`, prefer server-side tuning in `config/sky_archipelago-common.toml`, then restart and generate fresh chunks. Existing chunks will not reshape.

## Biome Mods

Sky Lands uses this biome source:

```json
{
	"type": "minecraft:multi_noise",
	"preset": "minecraft:overworld"
}
```

This is the best compatibility path for biome mods like Regions Unexplored or Oh The Biomes We've Gone when they extend the normal overworld biome preset. If a biome mod only injects into the literal `minecraft:overworld` dimension key, that mod may not affect `ckdm:sky_lands`; that behavior is controlled by the biome mod, not CKDM.

Rule: biome mods that modify the overworld biome preset should appear in Sky Lands; biome mods that target only the vanilla overworld dimension may need an extra compatibility datapack or mod-specific config.
