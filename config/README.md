# Config

Repo-local config examples and notes live here when needed.

Runtime configs are generated under the active Minecraft instance's `config/gisketchs_chowkingdom_mod/`.

For the current local playtest, use the Prism instance path named in `AGENTS.md`; repo `runs/` is not the source of truth for config curation.

Do not commit secrets.
